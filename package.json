{
  "name": "trash",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc",
    "dev": "vite",
    "build": "vite build",
    "prepare": "husky .husky"
  },
  "lint-staged": {
    "*.{md,css,html}": [
      "prettier . --write"
    ],
    "*.ts": [
      "prettier . --write",
      "eslint --cache --fix"
    ]
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.6.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0",
    "vite": "^6.3.6"
  }
}
