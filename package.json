{
  "name": "trash",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "prepare": "husky .husky"
  },
  "lint-staged": {
    "*.{md,css,html}": [
      "prettier --write"
    ],
    "*.ts": [
      "prettier --write",
      "eslint --cache --fix"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.0",
    "typescript-eslint": "^8.24.0",
    "vite": "^6.1.0"
  }
}
